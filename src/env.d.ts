// Constantes injetadas pelo Vite em tempo de build (vite.config.ts → define).

/** Versão do package.json. */
declare const __APP_VERSION__: string;

/** Commit curto do build no CI, ou "dev" em builds locais. */
declare const __BUILD_ID__: string;
